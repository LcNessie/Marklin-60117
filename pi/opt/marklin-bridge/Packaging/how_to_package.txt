To create a distributable package (.tar.gz) for this project:

1. Navigate to the project's root directory (`pi/opt/marklin-bridge`).
2. Run the following command in your terminal:

   python Packaging/package.py

This will create a 'build' directory (if it doesn't exist) containing the versioned application archive.